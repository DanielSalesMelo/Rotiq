{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install && cd server && npm install && npx esbuild index.ts --bundle --platform=node --target=node20 --format=esm --outfile=dist/index.js && cd .. && npm run build"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "node server/dist/index.js",
    "restartPolicyMaxRetries": 5
  }
}
